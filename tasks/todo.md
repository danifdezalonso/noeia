# Task Tracker

## Completed

- [x] Calendar page (`/dashboard/calendar`) — FullCalendar Week/Month/Day/Schedule views
- [x] Calendar height — `fixed inset-0` layout, `absolute inset-0` CalendarView
- [x] Calendar scroll fix — `datesSet` one-shot scroll to 9am
- [x] Calendar now-indicator — explicit `border-top` CSS fix
- [x] Calendar all-day row — halved to 12px
- [x] Calendar 24-hour range — slotMin/Max defaults
- [x] Sidebar collapsed icon centering — conditional `justify-center` classes
- [x] `ScheduleSessionModal` — sessions page CTA
- [x] `AddPatientModal` — patients page CTA + wired into patients.vue
- [x] Header `+ New` CTA — wired Session / Patient / Bill modals via `useGlobalModals`
- [x] `NewBillModal` — bill CTA from header

## In Progress
(nothing)

## Backlog
- [ ] Verify calendar height fix works in browser
- [ ] Messages page
- [ ] NoeIA page
- [ ] Dashboard home page
